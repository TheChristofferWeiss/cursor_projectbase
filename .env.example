# Supabase Configuration
# Get these values from your Supabase project dashboard: Settings → API

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anonymous/public key (safe to expose to client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (server-side only - NEVER expose to client!)
# Optional: Only needed for admin operations like user invitations/deletions
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Site URL (optional)
# Used for email links in production. Vercel sets NEXT_PUBLIC_VERCEL_URL automatically.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
